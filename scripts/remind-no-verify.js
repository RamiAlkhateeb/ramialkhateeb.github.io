#!/usr/bin/env node
'use strict';
// PostToolUse reminder: this project's convention is to NOT proactively verify
// changes in a browser/server after Edit/Write — only do so if the user
// explicitly asks (see .claude/skills/preview-site).
console.log('Reminder: per project convention, do not proactively serve/preview/screenshot this change — only verify in a browser if the user explicitly asked for it.');
