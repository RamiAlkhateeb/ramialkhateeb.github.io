# Project previews

Put each project GIF in this folder and update its `gif` path in `js/projects.js`.
Use lightweight, looped GIFs (or replace the paths with hosted media URLs) for the best page speed.
